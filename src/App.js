import logo from './logo.svg';
import './App.css';

function App() {
  return (
    <div className="App">
      <div className="div2">
        {Array.from({ length: 20 }, (_, index) => (
          <button className='grid'></button>
        ))}
      
      </div>
      <div className="div2">
      {Array.from({ length: 20 }, (_, index) => (
          <button className='grid'></button>
        ))}
      </div>
      <div className="div2">
      {Array.from({ length: 20 }, (_, index) => (
          <button className='grid'></button>
        ))}
      </div>
      <div className="div2">
      {Array.from({ length: 20 }, (_, index) => (
          <button className='grid'></button>
        ))}
      </div>
      <div className="div2">
      {Array.from({ length: 20 }, (_, index) => (
          <button className='grid'></button>
        ))}
      </div>
      <div className="div2">
      {Array.from({ length: 20 }, (_, index) => (
          <button className='grid'></button>
        ))}
      </div>
      <div className="div2">
      {Array.from({ length: 20 }, (_, index) => (
          <button className='grid'></button>
        ))}
      </div>
      <div className="div2">
      {Array.from({ length: 20 }, (_, index) => (
          <button className='grid'></button>
        ))}
      </div>
      <div className="div2">
      {Array.from({ length: 20 }, (_, index) => (
          <button className='grid'></button>
        ))}
      </div>
      <div className="div2">
      {Array.from({ length: 20 }, (_, index) => (
          <button className='grid'></button>
        ))}
      </div>
      <div className="div1">
        {Array.from({ length: 20 }, (_, index) => (
          <button className='grid'></button>
        ))}
      
      </div>
      <div className="div2">
      {Array.from({ length: 20 }, (_, index) => (
          <button className='grid'></button>
        ))}
      </div>
      <div className="div2">
      {Array.from({ length: 20 }, (_, index) => (
          <button className='grid'></button>
        ))}
      </div>
      <div className="div2">
      {Array.from({ length: 20 }, (_, index) => (
          <button className='grid'></button>
        ))}
      </div>
      <div className="div2">
      {Array.from({ length: 20 }, (_, index) => (
          <button className='grid'></button>
        ))}
      </div>
      <div className="div2">
      {Array.from({ length: 20 }, (_, index) => (
          <button className='grid'></button>
        ))}
      </div>
      <div className="div2">
      {Array.from({ length: 20 }, (_, index) => (
          <button className='grid'></button>
        ))}
      </div>
      <div className="div2">
      {Array.from({ length: 20 }, (_, index) => (
          <button className='grid'></button>
        ))}
      </div>
      <div className="div2">
      {Array.from({ length: 20 }, (_, index) => (
          <button className='grid'></button>
        ))}
      </div>
      <div className="div2">
      {Array.from({ length: 20 }, (_, index) => (
          <button className='grid'></button>
        ))}
      </div>
    </div>
  );
}

export default App;
